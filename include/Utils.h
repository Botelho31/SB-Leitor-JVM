#ifndef UTILS_H
    #define UTILS_H
    #include "Reader.h"

    class Utils{
        public:
            static std::string convertHexToString(std::string hexadecimalString);
        private:
    };

#endif